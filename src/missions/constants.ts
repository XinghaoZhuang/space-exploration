export enum MissionStatus {
  Succeeded = 'Succeeded',
  Failed = 'Failed',
  Preparing = 'Preparing',
  Launched = 'Launched',
  Unknown = 'Unknown',
}

export enum MissionSortType {
  Name = 'name',
  DateASC = '-_id',
  DateDESC = '_id',
}

